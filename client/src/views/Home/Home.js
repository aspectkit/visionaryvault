import React, { useState } from "react";
import "./Home.css";
import { useQuery, gql } from "@apollo/client";

function Home() {
  return (
    <div className="homeContainer">
      <h2>home</h2>
    </div>
  );
}

export default Home;
